@using AiWebGymTracker.Enums.PagesActionTypes
@using AiWebGymTracker.Infrastructure.Utils.WebRouters
@model AiWebGymTracker.Enums.PagesActionTypes.FoodPageActionTypes

@{
    ViewData["Title"] = "Training Page";
}
<div id="content">
    <div class="service-container">
        @foreach (var service in Enum.GetValues(typeof(FoodPageActionTypes)))
        {
            var (controller, action) = PageRouter.GetRoute((FoodPageActionTypes)service);
            <form asp-controller="@controller" asp-action="@action" method="post">
                <button type="submit" class="service">
                    <div class="icon"><i class="fas fa-plus-circle"></i></div>
                    <div class="text-content">
                        <h3 class="title">@service.ToString()</h3>
                    </div>
                </button>
            </form>
        }
    </div> 
</div>